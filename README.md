# Hospital-management-system
A simple Java console-based project that simulates how a hospital manages patients based on their priority levels — serious, non-serious, and general checkup. The system ensures that patients with higher urgency are served first using a multi-queue priority model. Concepts Used  (OOP) ,Collections Framework (Queue, LinkedList) , switch ,if-else...
